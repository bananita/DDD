using System;
using System.Linq;
using Core.Domain.Model.Delivery;
using Core.Infrastructure.Repository;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace Core.Infrastructure.UnitTests
{
    [TestClass]
    public class ClientRepositoryUnitTests
    {
  /*      [TestMethod]
        public void CommentsNumberIncreasesAfterSave()
        {
            // Arrange
            IBlogRepository r = new BlogRepository();
            var p = r.RetrieveAllPosts().First();

            // Act
            var liczbaPrzed = p.Comments.Count;
            r.InsertComment(p.Id, BlogObjectMother.CreateComment(2));
            var liczbaPo = p.Comments.Count;

            // Assert
            Assert.AreEqual(liczbaPrzed+1,liczbaPo);
        } */
    }
}
